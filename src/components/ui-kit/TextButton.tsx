import styled from '@emotion/styled'

import { NormalizedButton } from '@/components/ui-kit/Button'

export const TextButtonDeprecated = styled(NormalizedButton)`
  font-weight: 400;
  font-size: 1.4rem;
  color: ${(props) => props.theme.mercury};
`
